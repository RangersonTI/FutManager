
const URL_BASE = "/FutManager"

const Login = `/login`;

const Inicio = `/inicio`

const Partida = `/partida`

const Horario = `/horario`

const Jogador = `/jogador`

const Usuario = `/usuario`

const TipoUsuario = `/tipo-usuario`

const Punicao = `/punicao`

const FormaDePagamento = `/forma-de-pagamento`

const ContaAReceber = `/contas-a-receber`

export const Rota = {
    URL_BASE,
    ContaAReceber,
    FormaDePagamento,
    Horario,
    Inicio,
    Jogador,
    Login,
    Partida,
    Punicao,
    TipoUsuario,
    Usuario
}